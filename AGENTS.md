# 训练红线

1. 所有训练相关文档和代码必须位于 `train/` 文件夹内；根目录只保留本红线文件 `AGENTS.md`。任何训练、下载、清洗或数据转换前必须阅读 `train/TRAINING_PLAN.md`。
2. 训练只能使用 `train/` 中的代码；大文件只能写入 `/data/mini-k3/`，不得写入 SSD 项目目录。
3. 阶段、数据集版本/比例、清洗规则、tokenizer、超参或硬件发生变化时，必须在同一变更中同步更新 `train/TRAINING_PLAN.md`，记录日期、原因和影响。
4. 文档与代码、manifest 或运行参数不一致时，禁止启动训练。
5. 数据必须有来源、许可证、版本、脚本哈希、样本/token 统计和去污染报告；禁止未审核数据进入训练。
6. 启动训练必须使用文档命令和 manifest，先通过 smoke test 与短跑验证。
7. 未经用户明确要求，不得启动训练、下载数据、重启远程机器或修改系统服务。
8. 1M 推理能力必须同时通过无 cache/cache logits 等价测试、长文档评测和显存基准；仅提高位置上限或创建 cache 数据结构不视为完成。
